# General Environment
ENVIRONMENT=development  # or "production"

# Paths
DATA_PATH=./data/raw/dataset.csv
MODEL_DIR=./models/
LOG_DIR=./logs/

# FastAPI
API_HOST=0.0.0.0
API_PORT=8000

# Secrets (optional)
DB_URL=postgresql://user:password@localhost:5432/ml_db
AWS_ACCESS_KEY_ID=your_aws_key
AWS_SECRET_ACCESS_KEY=your_aws_secret

# MLflow (if used)
MLFLOW_TRACKING_URI=http://localhost:5000
MLFLOW_EXPERIMENT_NAME=default

# DVC remote (if used)
DVC_REMOTE_URL=s3://my-dvc-bucket/path/
